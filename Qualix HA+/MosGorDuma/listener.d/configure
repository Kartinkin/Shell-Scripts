################################################################################
################################################################################
# Внимание! Не стоит менять значение у следующих переменных.

# Имя монитора, как он описан в ${ORACLE_HOME}/network/admin/listener.ora
ListenerName=LISTENER

# Имя директории с управляющими файлами
# Директория создается при установке пакета программ
DevDir=${TOPDIR}/dev/listener

# Имя файла, в который будут записаны pid процесса listener
PIDsFile=${DevDir}/${ORACLE_SID}

# Переменная задает имена имя демона liestener.
MonitorProcess="tnslsnr"

# Имя файла конфигурации listener
ConfFile=${ORACLE_HOME}/network/admin/listener.ora

# Эта дурацкая утилита с непроизносимым именем
ListenerCtl=${ORACLE_HOME}/bin/lsnrctl
